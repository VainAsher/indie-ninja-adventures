# Planning Document — 2026-03-28

**Review scope**: HANDOVER.md (2025-12-12) vs current codebase vs ROADMAP_AND_PLAN.md (2026-03-25)
**Branch**: master (latest: `029b7e8` — biome expansion)
**Prepared by**: Claude Sonnet 4.6

---

## 1. HANDOVER.md Accuracy Assessment

The handover document is **4 months out of date** and no longer represents the project. It should be treated as historical reference only. Key gaps:

| Dimension | HANDOVER.md said (Dec 2025) | Actual now (Mar 2026) |
| --- | --- | --- |
| Phase 5 (Rendering) | 0% — next priority | ~65% done |
| Biome themes | 3 (dungeon, cave, building) | 7 (+ forest, town, sewer, hollow) |
| Pickup system | Deferred (component only) | Fully integrated (pickup_spawner, pickup_renderer) |
| AI / enemy system | Deferred | Fully integrated (enemy_ai, boss_ai, 6 boss types) |
| World gen tests | ~30%, deferred | Extensive (5 test files in tests/world_gen/) |
| File count | ~50 files estimated | 120+ files |
| Total lines | ~15,000 est. | ~54,290 |
| Mechanics | movement, jump, dash, crouch, wall_slide | + combat (3-hit combo), shuriken, teleport, ninjutsu |
| Campaign mode | Not mentioned | Full campaign: 30 missions, 6 regions, acts, endings |
| Dialogue/story | Not mentioned | story_manager, dialogue_system, cutscene_manager, ending_manager |
| Trading/economy | Not mentioned | trading_system, loot_system, shop_ui, items.json |
| Performance | No optimization done | O1–O10 optimization series complete |

**Recommendation**: HANDOVER.md needs a full rewrite to reflect v0.7.x reality. This is a M0 deliverable per the roadmap.

---

## 2. ROADMAP_AND_PLAN.md Milestone Status

### Milestone 0: Stabilization and Verification — ~75% done

Exit criteria: All P0 UATs pass + ROADMAP.md and SYSTEM_OVERVIEW.md updated.

| Deliverable | Status | Notes |
| --- | --- | --- |
| Run UAT suite and mark results | ❌ Not done | UAT_SUITE.md result columns are blank |
| Fix P0/P1 issues (camera shake, inventory, replay, map) | ✅ Done | Confirmed fixed per git log (replay fix `45f1dec`, perf series) |
| Align docs with reality | ❌ Not done | HANDOVER.md, SYSTEM_OVERVIEW.md, ROADMAP.md all stale |
| Build preflight / locked-file note | ❌ Not done | ISSUES_AND_RISKS.md flags this as medium risk |

**Blocker**: UAT hasn't been run. Without recorded results, M0 exit criteria are not met.

### Milestone 1: Progression and Encounters — ~20% done

Exit criteria: Boss mission UAT passes + ability gate UAT passes.

| Deliverable | Status | Notes |
| --- | --- | --- |
| Wire boss_manager into game loop | ❌ Not done | HIGH issue per ISSUES_AND_RISKS.md — code exists, not wired |
| Boss missions in data/missions.json | ❌ Not done | 30 missions exist, none have a `boss` field |
| Ability gates in level/hub generation | ❌ Not done | ability_gate.py exists, not referenced in game loop |
| Basic boss visuals and collision feedback | ⚠️ Partial | boss_ai.py + sprite infrastructure exists; not exercised in-game |

**Critical gap**: The boss system (boss.py, boss_ai.py, boss_manager.py) is the largest implemented-but-unwired feature in the project. Six boss types are defined; zero are reachable in gameplay.

### Milestone 2: Presentation and Feedback — ~50% done

Exit criteria: Combat feedback clear + audio settings functional and persistent.

| Deliverable | Status | Notes |
| --- | --- | --- |
| Sprite usage for enemies/NPCs | ✅ Done | animation_system.py, enemy_renderer.py, npc rendering integrated |
| Particle and hit feedback | ✅ Done | particles.py has dust/dash/impact; hurt_spritesheet exists |
| Audio playback wired | ❌ Not done | No pygame.mixer usage found anywhere in runtime path |
| Volume settings wired | ❌ Not done | config/settings.py has audio fields; nothing reads them at runtime |
| Debug hitbox toggle for combat | ⚠️ Partial | shuriken collision always visible (noted as low issue) |

**Critical gap**: Audio is completely absent. The settings menu has volume sliders that do nothing.

### Milestone 3: Controls and Accessibility — 0% done

| Deliverable | Status | Notes |
| --- | --- | --- |
| Key binding settings wired | ❌ Not done | config/settings.py has bindings; player.py uses hardcoded keys |
| Gamepad support | ❌ Not done | No controller input handling found |
| Accessibility toggles | ⚠️ Partial | Screen shake toggle exists in settings UI; partially wired |

---

## 3. Additional Issues (from ISSUES_AND_RISKS.md)

| Severity | Issue | Current Fix Status |
| --- | --- | --- |
| High | Boss system not wired into game loop | ❌ Unresolved |
| Medium | Ability gates not integrated | ❌ Unresolved |
| Medium | Settings (keybinds, fullscreen, vsync) not wired | ❌ Unresolved |
| Medium | Audio system missing entirely | ❌ Unresolved |
| Medium | Wall slide status inconsistency (docs say disabled, code has it active) | ❌ Unresolved |
| Medium | Docs materially out of date | ❌ Unresolved |
| Medium | Build can fail if output files locked | ❌ Unresolved |
| Low | Raycast test known to fail | ❌ Unresolved |
| Low | Shuriken collision box always visible | ❌ Unresolved |
| Verify | Camera shake intensity | Needs playtest |
| Verify | Inventory equip/use flow | Needs playtest |

---

## 4. Reported Bugs (Player-Observed, March 2026)

Six bugs were reported in this session. Investigation findings are below.

### Bug 1 — Sprite rotates/flips during attack

**Location**: [demo_game.py](../demo_game.py) ~L2898–2907, [rendering/animation_system.py](../rendering/animation_system.py)

**Root cause (likely)**: During an attack the player's `state.facing` can update from input handling mid-animation, causing the pre-flipped frame cache to switch direction frame-to-frame. Additionally, the wall-contact sprite inversion (`sprite_facing = -sprite_facing` when on a wall) can fire unintentionally during an attack that makes contact with a wall.

**Fix needed**: Lock `sprite_facing` to the value at attack start and hold it for the full attack animation duration. Disable the wall-inversion path while an attack is active.

### Bug 2 — Hurt and death animations don't play fully

**Location**: [rendering/animation_system.py](../rendering/animation_system.py) L84–86, [demo_game.py](../demo_game.py) L173–177, [game/health_system.py](../game/health_system.py)

**Root cause (certain)**: `hurt` animation is 3 frames at 12 fps = 0.25 s. Invincibility frames are 60 ticks = 1.0 s. The animation finishes and freezes on its last frame for the remaining 0.75 s. For death, the game likely resets or transitions scene before the 5-frame death animation finishes.

**Fix needed**: Either loop the hurt animation while i-frames are active, or reduce i-frame duration to match animation length. For death, block scene transitions until the death animation completes.

### Bug 3 — Jump animation frame assignment

**Location**: [rendering/animation_system.py](../rendering/animation_system.py) L76–77, L359–363, [demo_game.py](../demo_game.py) L204–213

**Root cause (likely)**: The sprite sheet's frame order may be inverted relative to the code expectation. The code extracts `raw[0]` for "jump" (ascending) and `raw[1]` for "fall" (descending). If the artist placed the falling pose first in the sheet, these are swapped.

**Fix needed**: Confirm sprite sheet frame order vs code. Either swap the extraction indices (`raw[1]` → jump, `raw[0]` → fall) or reorder the sprite sheet.

### Bug 4 — Victory/mission complete screen never triggers

**Location**: [demo_game.py](../demo_game.py) L2414–2526, [game/objective_tracker.py](../game/objective_tracker.py) L464–465, [game/mission_manager.py](../game/mission_manager.py)

**Root cause (certain)**: `level_complete` is only set in campaign mode when the player reaches the exit portal AND `mission_manager.exit_locked == False`. The `objective_tracker` is meant to call `mission_manager.unlock_exit()` when all objectives complete, but this wiring is either missing or broken — meaning the exit never unlocks, the portal is never passable, and the victory screen never renders.

**Fix needed**: Trace and repair the chain: `objective_tracker._on_objective_complete()` → check all objectives → call `mission_manager.unlock_exit()` → player hits exit → `level_complete = True` → victory screen renders.

### Bug 5 — Respawn without full health

**Location**: [entities/player.py](../entities/player.py) L448, [demo_game.py](../demo_game.py) ~L2156/2385/2521

**Root cause (likely)**: `player.reset()` correctly sets `current_hp = max_hp`, but the actual respawn code path in `demo_game.py` may not call `reset()` — it may only reposition the player without restoring health.

**Fix needed**: Confirm every respawn code path in `demo_game.py` calls `player.reset(spawn_x, spawn_y)` rather than just setting position directly.

### Bug 6 — Mission completion doesn't unlock further missions

**Location**: [game/mission_manager.py](../game/mission_manager.py) L363–409, [game/campaign_manager.py](../game/campaign_manager.py) L113–150, [demo_game.py](../demo_game.py) L2449–2467

**Root cause (certain)**: `mission_manager.complete_mission()` marks the mission as COMPLETED and saves to `campaign.completed_missions`, but nothing calls `campaign_manager` to evaluate which missions or regions should now unlock. `campaign_manager.can_unlock_region()` exists as a check function only — `unlock_region()` is never called automatically after completion.

**Fix needed**: After mission completion is recorded, iterate mission/region unlock conditions in `campaign_manager` and unlock anything whose requirements are now satisfied. Wire this into the `MissionCompleteEvent` handler or into the post-exit flow in `demo_game.py`.

---

### Bug Priority Order

| Priority | Bug | Impact |
| --- | --- | --- |
| P0 | Bug 4 — victory screen never triggers | Missions uncompletable |
| P0 | Bug 6 — mission unlock chain broken | Campaign progression blocked |
| P0 | Bug 5 — respawn without full health | Unfair / unplayable after death |
| P1 | Bug 2 — hurt/death animations freeze | Visual feedback misleading |
| P1 | Bug 1 — attack flip | Visual jarring, polish |
| P1 | Bug 3 — jump frame order | Minor visual inconsistency |

---

## 5. Proposed Work Plan

Work is organized in priority order. Phase 0 (bug fixes) must come first — the campaign loop is broken without it. Each phase follows the work loop (plan → tasks → commit each unit → update plan → push).

---

### Phase 0: Bug Fixes — Playable Campaign Loop

**Goal**: Fix the three P0 bugs and two P1 animation bugs before anything else. The campaign is broken without these.

**Tasks**:

1. Trace and repair objective → mission_manager.unlock_exit() → level_complete chain (Bug 4)
2. After mission save, call campaign_manager unlock evaluation to unlock subsequent missions/regions (Bug 6)
3. Confirm every death/respawn path calls player.reset() to restore full health (Bug 5)
4. Fix hurt animation: loop while i-frames active, or block state exit until animation completes (Bug 2)
5. Fix death animation: block scene transition until death animation completes (Bug 2)
6. Lock sprite_facing at attack start; disable wall-inversion during active attack (Bug 1)
7. Confirm jumpfall sprite sheet frame order vs code indices; swap if needed (Bug 3)
8. Run full test suite; run UAT cases UAT-BOOT-002, UAT-COMBAT-001, UAT-COMBAT-002 to verify

**Exit criteria**: Player can complete a mission → victory screen shows → next mission is selectable. Player dies → respawns at full health. Animations play correctly for hurt, death, jump, and attack.

**Commit strategy**: One commit per bug fix group (progression chain, health, animations).

---

### Phase 1: M0 Completion — Documentation and UAT

**Goal**: Close out Milestone 0 with verified pass/fail UAT results and accurate documentation.

**Tasks**:
1. Rewrite HANDOVER.md to reflect v0.7.x reality (7 biomes, full campaign, all systems, actual phase status)
2. Update docs/SYSTEM_OVERVIEW.md to match current systems list
3. Update docs/ROADMAP.md to reflect milestones from ROADMAP_AND_PLAN.md
4. Resolve wall slide doc inconsistency (confirm enabled/disabled status in code and align docs)
5. Add build preflight note in build/ README or build_production.bat (warn about locked files)
6. Run UAT suite against current build, record results in UAT_SUITE.md

**Exit criteria**: UAT result columns filled, all P0 cases recorded, HANDOVER.md no longer contradicts the codebase.

**Commit strategy**: One commit per doc updated, one commit for UAT results.

---

### Phase 2: Boss Integration — M1 Core ✅ COMPLETE (2026-03-28)

**Commit**: `4a9a096`

**What was done**:

1. ✅ BossManager wired into game_initialization.py + GateManager created
2. ✅ BossManager._handle_boss_death() fixed to emit typed BossDeathEvent (not raw dict)
3. ✅ BossManager update + player damage + render wired into demo_game.py play loop
4. ✅ boss field added to last mission per region in missions.json (6 boss missions)
5. ✅ Ability gates wired into hub via _rebuild_hub_gates(); portal interaction gated by region ability requirement
6. ✅ 11 tests added — all passing (spawn, death event, objective chain, data integrity, gate behavior)

**Notable fix**: ICE_QUEEN and DRAGON were missing from BOSS_DEFINITIONS — added both.

---

### Phase 3: Audio Foundation — M2 Core Gap ✅ COMPLETE (2026-03-28)

**What was done**:

1. ✅ `audio/audio_manager.py` — AudioManager with `play(name)`, `set_volume(vol)`, `load_sounds(path)`. Silent fallback if file missing. 12 named SFX slots.
2. ✅ `game/game_initialization.py` — `initialize_audio(sfx_volume)` added; calls `pygame.mixer.init()`, loads SFX from `assets/audio/sfx/`, silent if dir missing.
3. ✅ Combat hooks: swing on attack, hit_enemy on sword connect (enemy + boss), player_hurt / player_death on all damage paths (contact, lava, poison, arrows).
4. ✅ Environment hooks: jump (leaving ground with vy < −50), land, dash. Pickup: pickup_coin, pickup_item.
5. ✅ UI hooks: menu_select on up/down nav, menu_confirm on any action, inventory_open on toggle.
6. ✅ SettingsMenu — "SFX Volume" item cycles Off / 25% / 50% / 75% / 100%; writes `volume_sfx`; `apply_runtime_settings()` calls `audio_manager.set_volume()` on change.
7. ✅ `assets/audio/sfx/README.md` — documents all 12 expected filenames and format.

---

### Phase 4: Settings Wiring — M3 Foundation ✅ COMPLETE (2026-03-28)

**What was done**:

1. ✅ `entities/player.py` — `set_key_bindings(dict)` method added; `process_input()` uses `bound(action, default)` helper to check settings-bound key OR hardcoded WASD fallback.
2. ✅ `demo_game.py` — `_build_key_bindings()` maps settings string names → pygame constants; called at player creation and on every `apply_runtime_settings()` call.
3. ✅ `apply_runtime_settings()` — wires `show_hitboxes` → `show_debug_overlay`, `fullscreen` → `pygame.display.toggle_fullscreen()` (change-detected to avoid repeated toggling), `volume_sfx` → `audio_manager.set_volume()`.
4. ✅ `ui/menu_system.py` SettingsMenu — "Fullscreen" toggle added; "SFX Volume" cycling already added in Phase 3.
5. ✅ Settings persist via `GameSettings.save()` (called in `_set()` on every toggle).
6. ✅ `tests/test_phase4_settings_wiring.py` — 12 tests covering key binding resolution, player.set_key_bindings, save/reload persistence, and SettingsMenu item presence. All passing.

---

## 5. Phase Sequencing and Dependencies

```
Phase 1 (Docs/UAT)     — no dependencies, start immediately
Phase 2 (Boss)         — no hard dependency on Phase 1, can follow immediately
Phase 3 (Audio)        — no hard dependency, can overlap with Phase 2
Phase 4 (Settings)     — builds on Phase 3 (volume wiring), otherwise independent
```

Recommended order: 1 → 2 → 3 → 4. All four phases map directly to closing M0 and M1, and beginning M2 and M3.

---

## 6. Post-Plan Deliverables (after Phase 4)

When all four phases are complete:

1. **New UAT suite** — refresh UAT_SUITE.md with new test cases covering: boss encounters, ability gates, SFX, key binding changes, mission unlock chain, victory screen, and save/load persistence
2. **Updated ROADMAP.md** — reflect completed milestones and set the long-horizon goal

**Long-horizon goal (confirmed)**: A multiplayer game distributed via a custom launcher. The launcher will:

- Download and verify the correct client, server, and mod versions (pulled from GitHub releases)
- Load mods into the correct slots on client and server
- Ensure version parity between client and server before connecting
- Support offline play where applicable

This shapes the backlog: netcode/input pipeline work already started (network/ directory) is a foundation; the launcher is a separate deliverable built alongside or after multiplayer.

---

## 7. Out of Scope (Backlog)

The following are deferred. They feed into the long-horizon goal above:

- Music / soundtrack wiring (revisit after SFX stable)
- Gamepad support
- Accessibility toggles (text size, high contrast)
- Multiplayer / netcode (builds on existing network/ foundation)
- Custom launcher (client/server/mod distribution via GitHub)
- Modding UI and editor
- Advanced boss patterns / additional boss types
- Spatial partitioning performance pass

---

## 8. Branch Strategy

Each phase gets a feature branch off master:

| Phase | Branch |
| --- | --- |
| Phase 0 | `fix/campaign-loop` |
| Phase 1 | `docs/m0-completion` |
| Phase 2 | `feat/boss-integration` |
| Phase 3 | `feat/audio-foundation` |
| Phase 4 | `feat/settings-wiring` |

---

## Approval — Decisions Recorded

- [x] Phase 0 (bug fixes): all 6 bugs confirmed in scope
- [x] Phase 1 (docs/UAT): approved
- [x] Phase 2 (boss integration): approved
- [x] Phase 3 (audio): SFX only — music deferred to backlog
- [x] Phase 4 (settings wiring): approved
- [x] Phase sequencing: 0 → 1 → 2 → 3 → 4
- [x] Post-plan: new UAT batch + roadmap update with multiplayer/launcher end goal
- **Status**: APPROVED 2026-03-28
