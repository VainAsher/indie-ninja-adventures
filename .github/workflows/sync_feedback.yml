name: Weekly Feedback Sync (Legacy Bridge)

# Legacy bridge retained for manual runs only.
# Canonical intake sync now lives in:
#   VainAsher/indie-ninja-pipeline/.github/workflows/feedback_intake_sync.yml

on:
  workflow_dispatch:

jobs:
  legacy-sync-disabled:
    name: Legacy sync disabled
    runs-on: ubuntu-latest
    steps:
      - name: Control-tower routing notice
        run: |
          echo "This workflow is deprecated."
          echo "Run feedback intake sync from indie-ninja-pipeline instead."
          echo "Tracking issue: https://github.com/VainAsher/indie-ninja-pipeline/issues/2"
